import React, { Component } from 'react';
import FormView from './FormView';

class Form extends Component {
    render() {
        return (
            <FormView />
        )
    }
}

export default Form;